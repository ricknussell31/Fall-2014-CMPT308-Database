select name, city
FROM agents
WHERE name = 'Bond';

select pid, name, quantity
FROM products
WHERE priceUSD > 0.99;

select ordno, qty
FROM orders;

select name, city
FROM customers
WHERE city = 'Duluth';

